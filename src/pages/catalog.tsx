import { Flex } from "@chakra-ui/react"

function CatalogPage() {
    return (
        <Flex
        bgColor={'zinc'}
        color={'mustard'}>
            Página de catálogo
        </Flex>
    );
}

export default CatalogPage;